eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://api-service-registry.herokuapp.com/eureka
  instance:
    hostname: api-gateway-v1.herokuapp.com